#include <stdio.h>
float main(float a, float b)
{
    a = 43;
    b = 27;
    a *= a;
    b *= b;
    float c = a + b;
    float d = a - b;
    float e = a * b;
    float f = a / b;
    printf("%f, %f, %f, %f", c, d, e, f);
}
