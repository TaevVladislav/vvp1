#include <stdio.h>
#include <stdlib.h>
float main(float a, float b)
{
    a = -43;
    b = -27;
    a = abs(a);
    b = abs(b);
    float c = a + b;
    float d = a - b;
    float e = a * b;
    float f = a / b;
    printf("%f, %f, %f, %f", c, d, e, f);
}
